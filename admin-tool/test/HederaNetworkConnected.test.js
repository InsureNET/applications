const HelloHedera = artifacts.require('HelloHedera')

require('chai')
.use(require('chai-as-promised'))
.should()

// function tokens (n) {
//     retrun web3.utils.toWei(n, 'ether');
// }

contract('HelloWorld', ([ deployer, insured ]) => {
    let helloHederaContract

    before(async () => {
        // helloHederaContract = await 
        
    })


})